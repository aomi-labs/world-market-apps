# Share

Whole-message `share` or "show a friend". Call `render_share`. Paste `message` verbatim. Card if `image_status` is ready; else link-only — one message, no retry. Never the mandate engine. No referral code, count, reward, or named referrer. Deposit copy from the tool only.
